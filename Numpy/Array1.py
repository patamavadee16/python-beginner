import numpy as np
import time
from numpy import random
x=random.randint(1, size=(10))
print(x)
a = np.random(4); 
print(f"np.random.random_sample(4): a = {a}, a shape = {a.shape}, a data type = {a.dtype}")